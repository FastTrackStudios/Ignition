target/
target-*/
dist/
node_modules/
.DS_Store
.direnv/
result
result-*
*.esf3d
*.esf2
*.a3d
/.direnv/
__pycache__/

# Effect previews: 131 effects x 16 frames of 320x180 PNG is ~175MB, and
# PNGs do not delta-compress, so every regeneration would add that again
# to the history. They are a build artifact — deterministic from the
# profile, two minutes of GPU to remake (`just effect-previews`) — and a
# missing one degrades to a row with no thumbnail rather than an error.
# The looks' four stills stay committed; they are a quarter of a megabyte.
data/effects/previews/
data/macros/previews/

# The site's real Tailwind input, generated from `tailwind.css` with the
# git-dep paths cargo actually resolved substituted in — machine-local by
# construction. `just tailwind` remakes it. The compiled SHEET is
# committed, like the studio's and the Live view's: `asset!()` demands it
# at compile time, so a fresh clone and the nix build both have to work
# without running the recipe first. See apps/ignition-web/tailwind.css.
apps/ignition-web/.tailwind.gen.css
